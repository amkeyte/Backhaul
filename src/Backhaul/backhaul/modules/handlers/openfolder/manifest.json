{
  "id": "handlers.openfolder",
  "kind": "module",
  "description": "openfolder: URL-protocol handler (VBS + .reg + link-builder) — opens project folders in Explorer.",
  "requires": ["foundation"]
}
