"""Foundation — one shared, generic layer used by both services (BHT, BHW) and any
optional modules. Nothing domain-specific (ticket or wiki) lives here.

See migration/FOUNDATION_DESIGN.md for the full interface design.
"""
