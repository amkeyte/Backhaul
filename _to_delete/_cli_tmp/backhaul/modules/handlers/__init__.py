"""handlers — custom Windows URL-protocol handlers (editmd:, openfolder:) that let
Chrome-rendered markdown launch local Windows programs. Each handler is its own
sub-module since they're independently installable (VBS + .reg + link-builder each).
"""
