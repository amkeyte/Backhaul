# RM-0007 — Phase 2.11: Cross-ROM capability mechanism stress test

**Kind:** work
**Status:** resolved
**Created:** 2026-08-05 (backfilled)
**Owner:** Kofi
**DependsOn:** [RM-0006](RM-0006-phase-2-10.md)
**Ticket:** [0076](../../ClaudeWiki/Processes/Handoffs/0076-implement-phase-2-11-capability-stress-test-spec-md.md) — resolved; QA gate [0080](../../ClaudeWiki/Processes/Handoffs/0080-phase-2-11-qa-gate.md) — PASS
**Visualize:** <a href="../../ClaudeWiki/RoadmapGraph/Mockups/sample-visualization.html?focus=RM-0007" target="_blank" rel="noopener">Open in graph view ↗</a>

Deliberately adversarial stress test of the Cartridge capability mechanism — a clash probe and a
cycle probe, both meant to be rejected, plus one real shippable item (a Reputation ROM proving a
three-deep capability relay). Technically needed the Cartridge mechanism (2.7) and the multi-consumer
capability precedent (2.7.1) specifically — as of 2026-08-08 that need is represented via `RM-0010`
(see the dated note below), not as direct edges to `RM-0002`/`RM-0003`. Benefits from 2.10's
Loot↔D20System correlation landing first per the proposal's own sequencing note, still modeled as a
real dependency via `RM-0006`.

**Update, 2026-08-05:** ticket 0076 is in-review — Baraka reports all 11 acceptance criteria met, a
real content-modeling bug caught by a checked-in spike before it shipped, and two `ContentTests`
regressions ticketed to Nadia as **0079** (same pattern as 0070/2.10). **0079 is deliberately not its
own RM node** — same reasoning 0070 held to, it's routine test-compile fallout, not
roadmap-load-bearing. Status here stays `open`, not `resolved` — Kofi (this node's owner) hasn't
confirmed the ticket yet, and node status only flips on that confirmation, not on the assignee's own
claim.

**Resolved 2026-08-08.** Ticket 0076 closed on an Arryn-run `latest.trx` (181/181 passing); test-coverage
ticket 0079 (Nadia) and consolidation ticket 0078 also closed; QA gate 0080 (Lothar) verdict **PASS** —
see [`phase-2-11-verdict.md`](../../QA/phase-2-11-verdict.md). All 11 spec acceptance criteria
confirmed (one at fixture-level evidence rather than a live transcript, explicitly accepted in 0080's
resolution). Status flips `open` → `resolved` on Kofi's own confirmation of the QA verdict, per this
node's standing rule below, not on any assignee's self-report.

**Pruned 2026-08-08 (supersedes the note below): transitive reduction.** `RM-0010` also removed now
— `RM-0006` already requires `RM-0004`, which already requires `RM-0010`, so it was reachable without
a direct edge here too. Only `RM-0006` remains in `DependsOn`. Still `open` at the time of this note,
so a plain edit either way, not a dated-correction per the resolved-node discipline.

*(Prior same-day note, now superseded: "RM-0002 and RM-0003 removed... RM-0010 is now this node's
only direct ROM-tier link.")*

See [`../phase-2-11.md`](../phase-2-11.md).

## Required By

*(computed, hand-derived pending real tooling)* — [RM-0011](RM-0011-world-builder-operational.md)

## Related pages

- [RoadmapGraph proposal](../../ClaudeWiki/RoadmapGraph/proposal.md)
